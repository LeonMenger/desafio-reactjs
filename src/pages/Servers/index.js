import DefaultLayout from "../../components/DefaultLayout";

const ServersPage = () => {
    return (
        <DefaultLayout>
            <h2>Servers Page</h2>
        </DefaultLayout>
    );
};

export default ServersPage;
